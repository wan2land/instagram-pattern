{
  "name": "instagram-pattern",
  "version": "0.0.1",
  "description": "Perfect instagram regex pattern",
  "keywords": [
    "instagram",
    "hashtag",
    "regex"
  ],
  "author": "Changwan Jun <wan2land@gmail.com>",
  "license": "MIT",
  "repository": "https://github.com/wan2land/instagram-pattern",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "prepack": "npm run test && npm run build",
    "build": "rimraf ./lib && swc src -d lib --config-file ./build.swcrc && tsc",
    "test": "npm run build && npm run test:lint && npm run test:unit",
    "test:lint": "eslint src/**/**/*.ts",
    "test:unit": "jest src --runInBand"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.55",
    "@swc/core": "^1.2.130",
    "@swc/jest": "^0.2.17",
    "browserslist": "^4.19.1",
    "jest": "^27.4.7",
    "typescript": "^4.5.4"
  }
}
